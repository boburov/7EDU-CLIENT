"use client"

// imports liberays
import { useParams } from "next/navigation"

const page = () => {
  const path = useParams()

  return (
    <div className="container">
    </div>
  )
}

export default page